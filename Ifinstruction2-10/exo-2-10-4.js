/**
 * Réécrire 'if' en '?'
importance: 5

Réécrivez ce if en utilisant l’opérateur conditionnel '?' :

let result;

if (a + b < 4) {
  result = 'Below';
} else {
  result = 'Over';
}
 */

'use strict';

let result = (a + b < 4) ? 'below' : 'hover';